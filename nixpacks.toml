# Railway Nixpacks Configuration
# Uses Nix package for Chromium (not apt snap wrapper) so it works in containers

[phases.setup]
nixPkgs = ["chromium"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
